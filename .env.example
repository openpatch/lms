# Only needed if the game server runs on another machine than the site.
# Normally the client talks to the host it is served from.
# VITE_SERVER_HOST=localhost:3000

# Server-side. Required in production; see deploy/README.md.
# On the server these live in a .env file that pm2 reads via the ecosystem config.
# AUTH_SECRET=<openssl rand -base64 32>
# BASE_URL=https://lms.openpatch.org
